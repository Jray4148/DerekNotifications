# To build and run container locally
```aiignore
docker compose up --build
```
or 
```aiignore
docker compose up --build .
```

# Test local
```aiignore
curl -v http://localhost:8080/info/health
```

