services:
  server:
    build:
      context: .
      target: final
    environment:
      - AWS_REGION
      - AWS_ACCESS_KEY_ID
      - AWS_SECRET_ACCESS_KEY
      - YT_API_TOKEN
    ports:
      - 8080:8080
